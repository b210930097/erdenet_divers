name: erdenet_divers
description: "A new Flutter project."

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.5.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.3
  cupertino_icons: ^1.0.6
  camera: ^0.11.0+2
  image_picker: ^1.1.2
  pytorch_lite: ^4.2.7
  audioplayers: ^6.1.0
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/answer.json
    - assets/background.jpg
    - assets/checkList.png
    - assets/models/best.torchscript
    - assets/labels/text.txt
    - assets/distraction.mp3
    - assets/drowsy.mp3
